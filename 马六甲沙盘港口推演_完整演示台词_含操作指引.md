

## 第一部分：开场——这不是一个单港看板

各位好，我是温家懿。现在为大家演示的是设计和开发的“港航网络韧性数字孪生沙盘推演系统”。

这个系统是以马六甲海峡为示范场景，把港口、航道、船舶调度策略和微观单船验证组织成一个可以推演、可以解释、也可以回写结果的决策闭环。



---

## 第二部分：重要卡片、功能和设计初衷

### 1. 公开数据与顶部五项核心指标

左上角首先明确显示“公开实证数据”。这里没有把所有内容笼统写成“实时数据”。当前港口统计来自新加坡海事及港务管理局的公开月度数据；风浪和海流来自开放气象服务提供的模型场；船舶自动识别数据使用公开研究统计作为船流和船型基线；碳排放依据国际海事组织的燃料因子进行模型估算。生产级实时船舶自动识别系统、码头操作系统和船舶交通管理系统接口已经预留，但在没有授权时，系统不会把代表船舶冒充真实原始船位。


顶部五张卡片回答的是操作人员进入系统后的第一个问题：网络现在处于什么量级和什么状态。

### 2. 左侧四组卡片：对象、结构、通道和风险

左上是港航网络总览。它是先交代网络由多少港口、航道、锚地和船舶构成。
船舶态势展示不同船型的结构。船型结构直接影响港口资源需求
同样是一百艘船，船型组合不同，对港口造成的压力完全不同。

航道通航状态把每条通道的延误和拥堵等级直接列出来。

风险预警把气象、安全、环境和运营风险放在一起。在新加坡港、马来西亚巴生港和印度尼西亚杜迈港共同连接的马六甲海峡网络中，任一节点拥堵又会增加相邻锚地的等待和碳排。把这些风险统一表达，才能支持后面的联动推演

### 3. 中央地图和八阶段状态机

中央地图承担的是网络关系表达。

### 4. 右侧四组卡片：外部条件、绿色约束和关键节点

气象与海况卡描述船舶和港口无法忽略的外部约束。

碳排放监测把运营效率和绿色航运联系起来。。

拥堵热力图负责快速发现高压区域，关键节点监控则把热点落到具体港口。

### 5. 底部导航：从看见问题到执行决策

底部五个模块按照决策过程组织：态势总览负责看见问题，沙盘推演负责制造和计算情景，韧性评估负责衡量承压与恢复，调度优化负责比较动作，应急预案负责把高风险场景转换为可执行方案。（依次点击底部按钮）

这套页面结构的初衷，是让不同角色使用同一套事实基础：管理层看网络态势，调度人员看航线和窗口，安全人员看风险与预案，算法人员看状态、策略和验证结果。

-

（点击“沙盘推演”按钮。）

接下来进入本系统的核心演示。从扰动发生开始，完整走一遍“事件注入—策略推理—策略采用—单船验证—结果回写”的八阶段闭环。

---

## 第四部分：八阶段核心闭环

### 1. 建立可重复的基准状态

（依次点击“重置”““开始”按钮。）

我先重置场景，并启动推演。重置不是形式动作，而是为了建立可重复的基准状态。没有统一基准，就无法判断后面的指标变化究竟来自本次扰动、策略效果，还是上一次演示残留的状态。

### 2. 注入“新加坡泊位关闭”事件

（依次点击“事件注入”“新加坡泊位关闭”“确认注入该事件”按钮。）

现在我注入“新加坡泊位关闭”事件。泊位关闭看似发生在港内，但它首先降低服务能力，使到港波峰与泊位释放时间失配；随后排队会进入锚地，预计抵达时间和接续航线延误上升，最终可能影响相邻港口和后续班轮计划。


### 3. 打开已训练策略推理舱

（点击“RL策略推理”按钮。）

这里调用的是训练中心刚刚生成的真实检查点。如果尚未完成训练，系统会阻止推理并打开训练中心，不会调用一个写死的展示策略。
推理过程分成五步：状态离散化、检查点决策、动作价值归一化、单步模型投影和安全人工约束。四种强化学习策略读取真实更新后的价值表；模型预测控制则使用滚动时域优化结果。

左侧是当前状态张量。
中间给出的是概率分布，而不是假装未来只有一个确定答案。
（点击“能力损失”按钮。）


右侧展示不同动作的概率和收益对照。

### 4. 采用策略并查看下发结果

（点击“采用已训练策略”按钮。）

现在我采用当前检查点策略。系统会自动跳转到调度优化模块，
### 5. 返回沙盘，生成单船验证信息流

（点击“返回沙盘继续闭环”按钮；点击两次“推进 15分”按钮。）

策略采用以后，我把推演向前推进两个时间步，观察动作在网络中的执行结果。港口调度不能停在“模型推荐完成”，还要看船流、航道和节点压力是否按照预期演化。

（点击“生成信息流”按钮。）

现在点击“生成信息流”。系统把宏观策略封装成单船验证请求，其中包含验证对象、航线、目标航速、风险事件和调度动作。

### 6. 打开单船模拟器并回写结果

（点击“航行模拟器”按钮。）

这里真正打开了单船航行模拟器，用来展示船舶运动和微观验证能力。

现在把单船参考验证结果回写主沙盘。
到这里，顶部八个阶段全部完成。系统从公开数据和场景输入出发，经过事件注入、影响计算、韧性评估、训练后检查点推理、策略采用、单船接口、结果回写，最终形成指标和报告更新。

（点击“导出报告”按钮。）

最后可以导出闭环报告。报告记录数据证据、输入事件、计算结果、情景概率、动作分布、策略采用状态、单船请求和八阶段状态，使这次推演能够复盘，也能与不同扰动和不同策略进行横向比较。

---

## 第五部分：小懿人工智能全系统联动


刚才展示的是人工逐步操作。接下来演示小懿人工智能与整套系统的联动能力。

（点击“小懿已接入全系统”按钮。）

小懿当前接入了二十项受控页面动作，包括模块切换、启动和推进推演、事件注入、强化学习策略推理、训练中心以及报告导出。

它的定位不是一个只会回答问题的聊天框，而是一个受控的页面操作编排器。自然语言先被解析成已登记动作，再经过白名单、状态校验和步骤编排，最后才会逐步点击真实页面按钮。

（点击输入框。）

我现在输入“小懿，运行港口拥堵演示”，让小懿执行这条指令。

（点击“发送”按钮。）

小懿首先识别出“运行港口拥堵演示”，把它映射为已经登记的港口拥堵演示动作，并显示语义置信度。这里展示动作编号，是为了让自然语言和真实系统操作之间有一条可审计的映射，而不是让模型自由猜测页面位置。

接下来系统依次完成意图解析、动作白名单校验、操作链编排和页面执行。
这条指令最终被拆成三个真实步骤：进入沙盘推演、载入港口拥堵场景、点击开始。每一步执行前，小懿都会定位目标、检查按钮是否可见和可用、阻止重复点击，再等待页面状态回写。



### 2. 用小懿打开训练中心

（点击输入框。）

我现在输入“小懿，打开训练中心”。


## 第六部分：训练中心与小懿强化学习顾问

### 1. 介绍训练中心

这里是港口控制算法训练中心。它把生命周期拆分为数据校验、时间留出切分、环境与奖励装配、四种强化学习参数更新、模型预测控制辨识、留出集评估、检查点和测试回放。

训练中心的设计初衷，是让算法训练不再是后台黑箱。港口管理者可以知道训练使用了什么场景、优化什么目标、和哪些基线比较、是否通过离线评估，以及最终发布的是哪个模型检查点。

算法选择包含 Q-Learning、SARSA、Expected SARSA、Dyna-Q 和模型预测控制。前四种会执行真实价值函数更新；模型预测控制是控制理论基线，只做需求模型辨识与滚动优化，不伪装成强化学习。训练参数只保留当前引擎真正使用的价值更新步长、折扣因子、回合数、随机种子和奖励权重。

多算法对照的意义，不是为了把模型名称堆得更多。只有在相同训练段、状态空间、动作空间、奖励权重和留出测试段上比较，才能判断策略是否真正带来了稳定收益。

### 2. 选择港口目标，让小懿生成配置

（点击“训练优化目标”；点击“港口拥堵削峰”。）

我把训练优化目标选择为“港口拥堵削峰”。目标先于算法，是因为港口没有脱离业务目标的通用最优策略。削峰强调压制高压节点和拥堵扩散；公平排队强调避免某类船舶长期等待；低碳目标则会提高燃油与排放奖励权重。

（点击“小懿智能配置”按钮。）

现在让小懿根据目标配置训练。这里的小懿是强化学习专项顾问，它读取当前港口压力、航道状态、船型结构、优化目标和后端接入模式，再生成算法、基线、超参数、奖励权重和测试方案建议。

（点击“生成推荐”按钮。）

针对港口拥堵削峰，小懿推荐 Dyna-Q。原因是公开样例数据量有限，Dyna-Q 可以在真实交互更新之外复用已经学到的转移模型，提高样本效率；同时用模型预测控制作为独立的控制理论对照。

（点击“一键配置全部”按钮。）

现在点击一键配置。小懿会把建议同步到算法选择、基线对照、沙盘状态、训练参数、后台服务、策略测试和接口契约，而不是只在对话框里输出一段文字。

这些“小懿已配置”标记说明推荐已经真正写入训练表单。点击“启动训练”以后，进度条直接读取服务器已完成的回合和留出评估数量；界面只展示当前算法、环境步数、价值更新次数、奖励滑动均值、已访问状态和训练测试记录数。训练期间地图动画暂停，达到百分之百后才解锁真实留出集策略回放。

我特意给长时训练和重置动作保留人工确认。小懿可以帮助理解目标、组织步骤和配置参数，但高影响操作必须由人明确授权。对于港口生产系统，智能化的价值不是取消责任边界，而是让决策更快、更透明、更容易复核。

以上就是整套港航网络韧性数字孪生沙盘的完整演示。

我的演示到这里，谢谢您的观看。

---
