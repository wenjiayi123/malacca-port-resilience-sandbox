# 贡献指南

## 开发流程

1. 从 `main` 创建分支。
2. 执行 `pnpm install --frozen-lockfile`。
3. 修改后执行 `pnpm check`。
4. Pull Request 说明数据来源、协议变化、可复现随机种子和用户可见影响。

## 算法贡献要求

- 训练指标必须来自实际算法更新，不能由前端定时器、正弦函数或预置曲线生成。
- 训练集和测试集必须有明确边界；默认使用按时间留出，禁止随机打散造成未来信息泄漏。
- 新算法必须与现有基线共享同一环境、动作、奖励和测试记录。
- 控制理论、规则方法和 RL 必须如实分类。
- 结果必须包含数据指纹、种子、训练步数、参数更新次数和留出集指标。
- 使用港口私有数据时，只提交字段说明与匿名小样，不提交原始业务数据。

## 数据适配贡献要求

- 新字段应先加入 `docs/DATASET_CONTRACT.md`。
- 说明单位、时区、采样频率、缺失值处理、许可和是否允许再分发。
- 实时 AIS/TOS/VTS 接口必须从环境变量读取密钥，并提供断网/超时处理。

## 提交信息

建议使用 `feat:`, `fix:`, `docs:`, `test:`, `refactor:` 前缀。不要提交 `dist/`、
`.runtime/`、Godot `.pck`、软著材料、录制导出物或任何 Token。
