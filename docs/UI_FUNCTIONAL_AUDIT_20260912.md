# 2026-09-12 功能、按钮与联动审计

状态：**本轮本地审计完成。最新模拟器、独立启动及真实结果联动已验证；正常任务约 15 秒返回结果，风险请求保留降级结果。最终 195/195 测试、构建、当前运营验收和完整发布门禁通过。**

本轮检查覆盖本地 Web 运行界面、后端沙盘控制、训练与回放、证据读取、报告下载及小懿动作联动。通过实际点击检查画面、数据和状态变化，并用行为测试补查超时、重复操作、迟到响应、错误 JSON 等不易稳定手工触发的路径。表中列出的是本轮实际覆盖范围，不代表所有参数组合、所有设备尺寸或实船生产环境已经验收。

## 实际浏览器覆盖

| 范围 | 本轮操作与观察 | 结果 |
| --- | --- | --- |
| 主模块导航 | 态势总览、沙盘推演、韧性评估、调度优化、应急预案、证据与闭环，共 6 个模块 | 已逐个进入并检查内容切换 |
| 证据页面 | 实时遥测、数字孪生、预测模型、策略闭环、监管韧性、数据血缘、安全治理、模型版本、审计回放、现场适配，共 10 页 | 已逐页检查 |
| 大屏与演示入口 | 8 个面板放大、5 个演示预设、4 类事件注入 | 已操作并检查相关画面与反馈 |
| 后端场景 | 8 个运行场景、4 个监管场景 | 已检查场景变化与联动状态 |
| 运行控制与治理 | 启动、暂停；异常条件下阻止操作；FCFS 提案、两个测试身份审批、沙盘执行与回滚 | 已观察状态变化与执行/回滚回执 |
| 模拟地图 | 1280×720 下点击巴生港 | 原先被地图模式开关截获；修复后实际打开韧性评估 |
| 卫星地图 | 港口标记、弹窗坐标、AIS 状态；切换后的模拟对象；升级至 MapLibre 6.4.1 后在 `4186` 复验卫星纹理与 3D 地形 | 巴生港弹窗为 `2.99970, 101.39280`；升级后浏览器 warn/error 为 0；未授权 AIS 状态明确；卫星模式中的模拟地图 DOM 数量为 0 |
| 窄屏与地图缩放 | MapLibre 6.4.1 下 `390×844`，Sentinel-2 影像与 3D 地貌加载；恢复 `1280×720` 并切回模拟地图 | canvas 随容器为 `390×580`；文档 clientWidth / scrollWidth 均为 390，无横向溢出，浏览器 warn/error 为 0；恢复正常 |
| 小懿动作 | 重复要求打开设置、停止正在进行的动作 | 已打开设置保持打开；停止后阻断后续动作 |
| 训练与回放 | 创建真实后端训练任务，运行闭环、拥堵压力和天气测试；测试过程中重置 | 训练、闭环完整回放与三种评估请求已验证；压力回放中途重置有效 |
| 推理采用门禁 | 三种扰动后重新计算推理，再尝试采用未达条件的 MPC 结果 | 正常拒绝不合格结果，未误显示采用成功 |
| Godot 微观验证 | 资源发现、场景运行、请求送入与结果关联回写 | 当前源码 `dc0624f5` 重新导出；新版桥接真实回写、程序音频 Sample 警告消除。随后确认旧坐标映射使船舶初始位置落入新版地形，故此前 `failed` 结果只证明回写链路有效；坐标修复版实际 PCK 已通过正常/风险原生回归；新版浏览器正常请求真实 `passed`，warn/error 为 0；本地独立启动、正常回写、关闭与取消已验证，见后节 |
| 独立模拟器 | 从 `4186` 点击“独立模拟器验证”，运行中切换概览，完成后关闭；另启一项任务并取消 | 不创建 iframe；正常 `passed` / 14.952 s；事故封航 `degraded` / 16.008 s；关闭均保留完成结果；取消无结果且窗口关闭 |
| 文件导入与下载 | 闭环报告、核心/业务决策报告、Godot 结果导入 | 已实际下载并解析闭环草稿、核心执行/回滚、业务审批报告，状态和提案一致 |

在新增独立启动入口之前，静态代码盘点另识别了 140 个原生按钮模板、21 个自定义点击目标和 4 个额外指针目标。这些是模板或事件绑定计数，含条件渲染与重复实例，不能相加称为逐一点击通过的按钮数。实际操作覆盖以本表和浏览器记录为准。

## 本轮训练与评估读数

实际完成任务：`rl-20260912090921-e55c9fb0`。配置为每种 RL 算法 120 episodes，包含 Q-Learning、SARSA、Expected SARSA、Dyna-Q 四种 RL 算法及 MPC 对照。MPC 是对照算法，不计为第五种 RL 训练。

| 测试 | 本轮界面/后端观察值 |
| --- | --- |
| 闭环回放 | 57 步，进度 100%，reward 3.419 |
| 峰值拥堵压力 | reward 3.378；延误 0.34 h → 0.11 h |
| 天气扰动泛化 | reward 3.252；风险指标 7.57% |

这些数字对应上述单次本地沙盘任务和测试配置，用于确认按钮触发了真实后端工作、结果能回到正确界面。它们不是实测港口 KPI，也不替代冻结冠军模型的独立训练、测试和收敛报告。

## 已修复的问题及回归依据

| 问题 | 修复行为 | 验证依据 |
| --- | --- | --- |
| 一个证据接口失败会掩盖其余接口结果；旧刷新覆盖操作后状态 | 独立展示接口错误；保留遥测时明确标为上次快照；失联禁用依赖操作；取消已过期刷新 | `tests/operationalEvidenceCenter.test.ts`：独立失败与恢复、场景注入后旧响应迟到 |
| 审批、执行或回滚后旧报告仍留在联动证据中；重复交班误认旧 ready | 生命周期变更清空旧报告；执行状态控制按钮；交班结果绑定本次时间与关联 ID | 同上：报告失效、重复执行阻止、失败交班不能复用旧结果 |
| 报告下载接收错误完成状态或其他提案数据；点击失败时 URL 未释放 | 两类报告验证协议、时间、哈希格式、当前 proposalId、完成状态与审批/执行一致性、模拟权责；15 秒超时；成功发起下载后才发布报告；失败清旧报告并释放 URL | 同上：核心及业务真实适配器响应、下载 JSON/文件名、坏状态、错提案、click 抛错和 URL 回收 |
| 地图模式开关截获港口点击；卫星层下仍有模拟控件；总览底栏过高 | 为模式切换预留独立区域；总览底栏缩短；卫星模式卸载模拟对象；证据模块背后地图不可聚焦；容器尺寸变化触发地图重绘 | 浏览器巴生港点击复验；卫星模式模拟 DOM 为 0；MapLibre 生命周期回归 |
| 训练界面或系统设置被其他层挡住，按钮看似无响应 | 修正训练、Godot、系统设置层级，训练监控面板随内容滚动 | 浏览器滚动至测试入口后实际点击 |
| 训练/评估重复提交或重置后迟到响应改写新状态；旧回放时钟继续推进 | 请求与任务、算法、测试场景绑定；取消、超时和服务切换可见；回放定时器校验当前测试 | `tests/appTrainingRuntime.test.ts`、`tests/rlServiceEndpoint.test.ts`；实际训练与压力测试重置 |
| 外部顾问参数与界面设置不一致；坏数值可能触发 `.toFixed` 异常 | 补齐六项奖励、调参和验证设置映射；约束种子范围；验证返回协议和 UI 读取字段 | `tests/xiaoyiLinkageRuntime.test.ts`、训练相关行为测试 |
| 小懿重复打开动作反向关闭目标；旧成功结果被当作本次完成 | 使用目标状态确认；新响应修订标识；停止后阻止后续定位与执行 | 24 个动作标签映射与取消/等待行为回归；设置重复打开实测 |
| Godot 缺失导出被 SPA 的 HTTP 200 误判可用 | 校验实际 HTML 配置、JS/WASM/PCK 类型与长度；8 秒有界检查；补 PCK MIME | `tests/godotValidationAdapter.test.ts`：SPA 200、缺件、错误 MIME、空/截断资源、完整导出、压缩响应、真实挂起 HTTP 请求超时 |
| Godot 浏览器 `user://` 目录定位错误，场景运行却未回写 | 桥接识别实际 `/userfs/godot/app_userdata/<app>`；有项目标题时必须精确匹配项目目录，避免误用旧版本目录；请求/结果按关联 ID 验证 | `tests/godotWebBridge.test.ts`、`tests/productionServer.test.ts`；音频修复版已真实回传，保留原始 `failed` 状态。该次失败由后续确认的坐标映射缺陷触发，不能作为危险场景正确拒绝的验收证据 |
| 使用过期 Godot 导出；导出失败可能留下混合资源 | 从当前源码隔离复制后导出，记录源码提交、兼容补丁和 HTML/JS/WASM/PCK 长度及哈希；完整验证后替换，保留旧导出并支持失败恢复 | `tests/godotExportTransaction.test.ts`；旧导出备份保留，原航行模拟器源码未被 Web 兼容补丁改写；最终坐标版资源已校验 public/dist 与审计 manifest 的全部 HTML/JS/WASM/PCK 长度、哈希一致 |
| 大型 Godot PCK 请求占用整包内存，HEAD 也读取整包 | HEAD 只读取文件信息；GET 以流传输，并处理客户端取消 | `tests/productionServer.test.ts`；包含 256 MiB 文件的流式传输与取消后服务可用回归 |
| MapLibre 5.24.0 命中严重依赖漏洞 `GHSA-jrc7-96c5-q579` | 精确升级最低修复版 6.4.1，并适配 ESM 导入与 worker；新增 v2 依赖证据，保留 v1 报告和原环境快照 | `tests/dependencySecurityUpgradeV2.test.ts`；升级后真实卫星纹理、3D 地形、巴生港弹窗复验通过；最终依赖审计零已知漏洞，完整发布门禁通过 |
| 非终态、错请求或矛盾 Godot 结果可能结束闭环；导入读取期间换请求；模拟时钟回退 | 验证终态和数值一致性，核对 iframe 来源及请求/船舶 ID；读取完成后重新核对当前请求；每次请求使用独立 UUID；时钟回退拒绝旧结果 | Godot 适配器回归、`tests/appReportImportRuntime.test.ts` |
| 报告携带后端令牌；Godot 完成误将未执行的策略推理标成完成 | 导出清空认证令牌；推理阶段只读取实际推理状态；草稿明确缺失步骤 | 11 项真实处理器回归；此前实际下载报告为 6/8，明确缺少事件注入和策略推理 |
| 闭环阶段已完成却仍显示“待启动”；初始事件感知使用默认 2024 时间而遥测为 2026 | 阶段首次从 pending 完成且没有显式开始时间时补齐开始时间；保留既有时间。首次无交互同步时把事件感知校准到当前数据时标；重置立即使用当前 baseScenario 时间初始化，避免等待 30 秒遥测轮询；首次同步与重置均通过已有格式化函数显示 +08:00 时间，避免原始 UTC ISO 与头部时间混用 | `tests/appReportImportRuntime.test.ts`：11 项通过，覆盖有效导入开始时间与完成时间一致、实际重置处理器立即采用当前时标；最终浏览器首次同步与重置后均即时显示 `2026-01-04 15:00:00`，无 2024 默认时间或 UTC ISO 文本 |
| 内嵌 Web 初次加载大型 PCK 和 WebAssembly 等待较长 | 新增“独立模拟器验证”，完整请求直接交给本机 Godot 运行已验收的最新 PCK；内嵌入口明确为“内嵌预览”，原估算入口明确为“快速估算回写” | 浏览器实际启动无 iframe，原生窗口显示并返回真实结果；14.952 s 单次任务读数，不作为跨设备加速比例 |
| native 启动时旧演示结果可能仍显示已完成；迟到请求、重复点击和取消可串任务 | 启动先清旧结果与完成阶段；1 秒轮询；8 秒网络超时、120 秒前端等待上限；按请求/船舶/任务核对，只取消自己创建的任务；关闭期间保持 busy | `tests/appReportImportRuntime.test.ts` 与 native adapter/controller 22 项行为测试；真实启动、完成关闭、运行取消复验 |
| native 取消过程或服务销毁与异步创建/读盘交错 | 取消后读盘不能改回 completed；服务销毁后不能再启动进程；本机同源、输入约束及配置令牌继续生效 | 独立 API/生命周期审查 19/19，先前取消与销毁竞态失败产物保留；该审查为 headless，与真实可见窗口证据分别列示 |
| 用户通过独立窗口自身关闭按钮退出后，网页仍显示窗口打开 | 结果完成后每 5 秒同步本任务的 windowOpen，最多 30 分钟；连续 3 次失败则停止监测并保留完成结果；换请求、重启、取消或卸载停止旧监测，不重复回写 | native controller 18 项 + adapter 4 项回归通过；通过所属任务 API 模拟外部退出后，网页自动同步关闭且保留原结果，未声称实际点击 OS 关闭按钮 |
| 事件日志使用 UTC，主时钟与碳图使用马来西亚时间 | 事件时间通过已有 +08:00 格式化显示；碳图范围来自实际数据小时 | `tests/operationalSimulator.test.ts` 跨日期回归；界面数据检查 |
| 兼容的界面修复使旧源码指纹失配，核心 v2 自动回退 | 新增明确范围的源码兼容证据；继续核验原模型、学习源码、数据和历史报告哈希 | `tests/runtimeCompatibilityEvidence.test.ts`：v2 恢复；篡改模型/学习代码/扩大模拟器修改范围仍拒绝 |

新版地形与原固定地理坐标映射不兼容，曾使无风险请求在开始时进入真实地形 AABB。本轮保留 `-22.58 m` 和 `-14.91 m` 原始失败结果及旧音频导出 manifest。最终兼容层基于当前场景的 32 个地形包围盒、6 个岩石和场景边界，为整段航路搜索可容纳的平移；保持航向、长度和进度，之后再按原请求注入风险。场景地形、风险和历史策略没有被删除或改写。无法找到可用位置时返回明确失败，并清空上次碰撞统计。

## 最新 Godot 导出与原生回归

导出使用 Godot `4.7.1.stable.official.a13da4feb`、`Malacca Web Simulator` 预设和原模拟器当前提交 `dc0624f5ee3b0bcbeb21f5c610b670bc8bb3778f`（提交时间 `2026-09-07T14:29:05+08:00`，源码目录 `dirty=false`）。Web 音频与坐标补丁只作用于隔离副本。最终导出时间为 `2026-09-12T10:00:59.713918+00:00`，即马来西亚时间 18:00:59。

PCK 为 **588,169,244 字节**，SHA-256：`8b461d67a225d19fef03afb13770adce89e68486344756e0fa9b84eae3e3e473`。已逐一读取 `public/godot-simulator` 和 `dist/godot-simulator` 的 HTML、JS、WASM、PCK 及 manifest，长度与哈希均匹配最终审计 manifest。证据位于 `reports/artifacts/ui-runtime-compatibility-v1/godot-export-manifest.json`。

实际导出 PCK 的 10 项映射不变量及 3 个原生场景回归均通过，未使用 headless 立即完成分支：

| 原生请求 | 实际终态 | 最小间距 | 窗口 | 风险对象 |
| --- | --- | --- | --- | --- |
| `coordinate-check-normal`，`vessel-001` | `passed`，safePass=true | 228.80 m | 2.11 s | 0 个临时障碍 |
| `coordinate-check-original-risk`，`vessel-001` | `degraded`，safePass=false | 10.44 m | 2.06 s | 保留原请求的 2 个临时障碍 |
| `coordinate-check-injected-closure`，`vessel-001`，进度 99% | `failed`，safePass=false | -1.65 m | 2.01 s | 保留封航请求的 1 个临时障碍 |

原始结果保存在 `reports/artifacts/ui-runtime-compatibility-v1/godot-coordinate-validation.json`。另一个独立 Godot 探针完成 **19/19**：完整航段与薄障碍/切线、边界、无解和无效几何、平移后方向/长度/进度、四种风险仅平移一次、备用终点一致、失败不复用旧碰撞计数。先前 17/19 的两项失败和修复后结果一并保留在 `godot-coordinate-independent-review/`。

这证明约 2 秒验证窗口内的正常、降级与拒绝行为；不证明已完成全航程。有限网格搜索仍可能漏掉狭窄可行走廊，未宣称全局最优；`geographicCalibrationVerified=false`，这里是仿真地形中的航路嵌入。最新 PCK 的正常浏览器请求 `malacca-demo-normal-transit-20260104150000-edebdbeb-7cd3-4774-bcb0-dd4727bfd0b1` 已真实回写 `passed`，新版场景已实际显示并检查截图，warn/error 为 0。该次导出界面显示草稿 7/8、缺少策略推理，文件名为 `malacca-closure-report-draft-incomplete-2026-09-12T10-07-43-665Z.json`；但尚未在下载目录确认文件，**不计为已经落盘解析的下载证据**。

报告下载的 `auditHash` 在客户端检查格式，未宣称客户端重新计算或认证服务端签名。对应审计链完整性仍由服务端验收和独立门禁验证。

## 模型与历史证据保留

没有用本轮 UI 演示训练替换现有冠军模型，也没有为恢复 v2 重新训练或重写历史验收报告。新增 `reports/dependency-security-upgrade-v2.json` 组合原 v1 依赖证据与 MapLibre 6.4.1 变更，不覆写历史依赖报告。新增 `reports/ui-runtime-compatibility-v1.json` 记录允许的兼容修改和原始源码快照；模型注册表仍检查模型、保留训练产物、数据及学习代码哈希。`server/operationalSimulator.ts` 只允许事件时间显示表达式的修改，公共证据接口只允许本轮明确的顾问提示文本修改。Godot Web 坐标与音频补丁的隔离副本另由导出 manifest 记录。

当前验收通过 `scripts/operations/buildUiRuntimeAcceptance.ts` 在临时副本运行原运营验收，保存为新的 `reports/artifacts/ui-runtime-compatibility-v1/operational-current.json`，不覆盖历史 `reports/operational-closure-acceptance-v2.json`。最终兼容扩展生成于 `2026-09-12T10:41:42.717Z`；当前验收生成于 `2026-09-12T10:42:48.930Z`，绑定 175 项源码和 195 项通过的检查日志，运行模型为 ACTIVE_V2、未重训。175 项源码、保留模型/数据/报告及新的运营回执哈希均已逐项核对。此前 166/168/189 项中间检查及已修复的隐私扫描失败记录保留为过程证据，不用于冒充最终门禁。

开始审计前已有的 `docs/DEMO_GUIDE.md`、`scripts/demo/check_environment.sh`、`scripts/demo/start_web_demo.sh` 和 `scripts/demo/resolve_web_runtime.sh` 修改予以保留。没有将它们重置为仓库版本。

## 验证方法与外部依赖边界

行为测试使用实际组件处理函数、适配器和受控网络/定时器，检查状态、JSON、错误和资源清理。它们不能替代浏览器的命中测试、布局和真实 WebGL 场景验证；浏览器表格中的项目由本轮真实操作单独确认。

卫星底图依赖外部瓦片服务、网络和 WebGL；授权 AIS 数据、本地小懿模型、外部 RL 服务及 Godot 导出依赖各自配置与可用性。接口不可达、授权缺失或资源缺件时应显示失败或缺失状态。未配置的依赖不能因为本地页面可打开而标记为已经接通。

所有结论保持 `simulation_mode=true`、`live_data_verified=false`、`dispatch_allowed=false`、`production_authority=false`。双人审批使用本地测试身份，执行和回滚回执属于沙盘。没有验证现场实名审批、设备控制、正式海事/海关授权或实船生产调度。

## 独立模拟器实际启动与回写

用户允许直接向独立模拟器传递数据后，新增本地启动入口。服务读取并校验上节同一个最新 PCK，以独立请求目录启动本机 Godot；完整请求通过 JSON 传入，原生窗口的真实结果经同源接口回到当前船舶闭环。浏览器保持可操作，不再为这个入口挂载 Web iframe。界面另保留“内嵌预览”和明确标为“快速估算回写”的原估算入口。

在 `http://127.0.0.1:4186/` 实际点击正常通航的独立验证：

| 项目 | 实际结果 |
| --- | --- |
| 任务 ID | `3cb60dc7-a8c5-4824-95b4-c7effd597659` |
| 请求 ID | `malacca-demo-normal-transit-20260101023000-acc2bdf4-85fc-406c-afdb-77376d52388c` |
| 任务创建 → 真实结果 | `2026-09-12T10:25:04.055Z` → `10:25:19.007Z`，**14.952 s** |
| 场景与结果 | macOS 原生窗口；`passed`，最小间距 89.43 m，物理验证窗口 2.13 s |
| 页面响应 | iframe 数量 0；启动期间能切换概览 |
| 完成后关闭 | 同任务仍为 `completed`，`windowOpen=false`；结果逐字段保留 |
| 运行中取消 | 初次任务 `05a60dad-6445-43a3-95d9-4606ef9a8de4` 及最终任务 `3d38268a-1ee0-4ec2-b4f2-a52c058c1f21` 均为 `cancelled`，`windowOpen=false`，无 result；最终页面明确“本次独立验证已取消，未生成验证结果。” |

浏览器对应原始任务、关闭/取消回执、时序与场景截图位于 `reports/artifacts/ui-runtime-compatibility-v1/native-browser-normal*.json`、`native-browser-normal.png`、`native-browser-cancelled.json`，哈希已记录在 `browser-verification.json`。14.952 s 是这台本机的单次任务创建到真实结果时间，包含场景装载；不是对所有设备的性能承诺，也没有据此计算加速倍数。

事故封航请求另经真实浏览器送入独立窗口，任务 `b1f913dc-eb31-444a-ac69-5c9204238a51`，请求 `malacca-demo-accident-closure-20260102080000-b9eaf99d-94b0-4416-8d48-6ccfd82e839a`，船舶 `vessel-004`。任务创建到真实结果 **16.008 s**；2.01 s 验证窗口内结果为 `degraded`、safePass=false、risk=critical，最小间距 18.92 m、碰撞/搁浅均 0、建议 6 kn，原始碳变化 -0.0002 t。页面按正确请求回写并完成对应阶段；关闭后仍为 completed、保留结果、窗口关闭。原始证据为 `native-browser-risk.json`、`native-browser-risk-closed.json`、`native-browser-risk-timing.json` 和 `native-browser-risk.png`。

原始 Godot HUD 的“任务进度 100% NEXT WP-02”按当前目标航点索引除以航点段数显示，两点航线进入第二个目标航点时即可显示 100%；它不是已航行距离或已抵达证明。该截图同时仍显示距 WP-02 约 68.5 m、ETA 00:15，原始回执明确 `reachedDestination=false`。HUD 的 `SAFETY CLEAR` 指即时碰撞告警，也不替代单船验证的安全间距门禁。本次仅确认 2.01 s 窗口的 `degraded` 结果，不宣称完成全程；保留原始 HUD 和 PCK，未为此重导出场景。

最终状态区经浏览器测量为 flex 布局，状态栏高 44 px，操作区高 187 px、overflow=auto，未观察到重叠。最终取消场景 warn/error 为 0。原始 -0.0002 t 曾在界面一位小数显示成 -0.0 t，现由统一显示函数在小于 0.1 t 时最多保留四位并去尾零、普通量级保留一位，舍入后的负零归零；原始及恢复调整后的 Godot 展示均已统一，JSON 数值不变。相关格式函数与 App 处理器合计 12 项回归通过。最终真实任务的原始 0.0008 t 在卡片显示 `+0.0008t`，阶段显示 `0.0008t`，没有被舍入成零；最终门禁已覆盖该修正。

独立 API/生命周期审查在 `native-independent-review/` 保留 19/19 结果及此前两项竞态失败，覆盖本机同源、外部来源、Host 欺骗、令牌、输入范围、取消后迟到读盘和销毁后迟到启动。前端 adapter/controller 另有 22 项行为回归。原生启动前会清除旧演示或估算结果；任务完成与沙盘接受回写分别展示，数据时标回退时仍能拒绝回写。关闭已完成窗口保留结果；取消无结果的运行不声称已有验证结果。最终布局和这条取消提示的浏览器复验均通过。

最终显示与关闭状态再次实测：任务 `25371e1d-b968-4f91-95b5-a584566aac2d`、请求 `malacca-demo-normal-transit-20260103093000-71607ec5-f2c9-44b5-8f40-2a9f30382dcc`，15.517 s 返回 `passed`；最小间距 104.81 m、物理窗口 2.02 s，iframe 数量 0。通过所属任务 API 关闭已完成窗口，模拟页面之外的退出后，网页自动显示“独立窗口已关闭，已有验证结果已保留。”，关闭按钮数量为 0，原始 result 逐字段不变，warn/error 为 0。此项未实际点击操作系统窗口的 X。证据为 `native-browser-final-normal.json` 与 `native-browser-external-close.json`。

完成后的窗口状态每 5 秒检查一次，最多 30 分钟；连续 3 次读取失败停止监测并保留结果。换请求、重新启动、取消或卸载均停止旧监测；状态观察不重复触发结果回写。

独立入口需要本机服务和 Godot，可用性不足时显示明确错误；不会静默切换到内嵌 Web。接口仅发同源 Bearer，轮询和关闭沿用任务启动时的令牌，不将令牌写进请求 JSON 或报告。

## 最终验收记录

以下数据对应最后一次源码冻结后的完整检查，退出码均为 0。

| 条目 | 最终结果 |
| --- | --- |
| 功能/按钮覆盖 | 6 个主模块、10 个证据页、8 个放大面板、5 个演示预设；1280×720、390×844、1920×1080；另有独立启动、正常/风险回写、关闭、取消。静态模板数不作为逐一点击完成数 |
| 当前源码与验收 | 175 项源码 SHA 全部一致；`reports/ui-runtime-acceptance-v1.json`：`d8ea87a13f0103e7851c5766e9f5be0f415f745bfe28e38a4d3951f203e547d1` |
| 兼容扩展 | `reports/ui-runtime-compatibility-v1.json`：`90947432df7fa79c5446eece4bbc444a8b6a755207105355231ebc18319b1ae4`；模型、学习源码和历史证据保留 |
| `pnpm check` | **195/195，失败 0，构建 3.03 s**；日志 `/tmp/malacca-ui-native-final-check-20260912.log`，归档 `reports/artifacts/ui-runtime-compatibility-v1/pnpm-check.txt` |
| 当前运营增量验收 | `UI_RUNTIME_ACCEPTANCE:PASS:195_TESTS:ACTIVE_V2:IMMUTABLE_HISTORY`；新的 `operational-current.json` 包含执行、回滚与完整审计验证 |
| `pnpm release:check` | **通过，退出码 0**；依赖审计零已知漏洞、公开隐私扫描通过、历史与当前证据验证通过、运行包 ACTIVE_V2 来源核验通过、项目完整性通过 |
| 发布门禁日志 | `/tmp/malacca-ui-native-final-release-20260912.log`；归档 `reports/artifacts/ui-runtime-compatibility-v1/pnpm-release-check.txt`，SHA `8197c967ea42009890c8c7f4cf197eaac0e52e8738423895d427136c6b8bb739` |
| Godot 导出 | Godot 4.7.1，最新原源码 `dc0624f5`，PCK `8b461d67…3e473`；public/dist 全部核心资源与 manifest 再次逐项核对 |
| Godot 回归 | 实际 PCK 的 10 项映射不变量 + 正常/风险/封航 3 场景；独立坐标探针 19/19；独立 API/生命周期 19/19；真实正常、降级、取消、关闭和外部退出同步通过 |
| 浏览器下载 | 5 个此前已下载文件核对哈希、状态和提案关联；后续 Web 导出尝试未确认落盘，明确不计入这 5 个 |
| 最终本机状态 | 原航行模拟器源码 `git status` 为空；`4186` 的 native/status 可用，Godot 4.7.1、来源 `dc0624f5`，无 activeJobId；测试原生窗口均已关闭，网页保留正常结果供查看 |
| 审计产物 | `browser-verification.json` 保存回执关联、原始失败和 53 个其余审计文件的 SHA-256 清单；两个独立审查目录的清单哈希均核验通过 |

`git diff --check` 通过。本轮只完成本地修改与验收，没有据此宣称已经发布到远程仓库、接入实船或取得现场调度权限。仿真边界与未覆盖组合以本文件开头及验证边界为准。
