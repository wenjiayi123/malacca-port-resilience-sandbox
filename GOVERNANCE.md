# 项目治理

## 变更级别

- 普通 UI/文档：至少通过 `pnpm release:check`，界面改动需浏览器验收。
- 数据合同/算法/奖励：必须附固定种子、数据指纹、Train/Validation/Test 证据和兼容性说明。
- 安全/生产接口：需要第二位维护者复核，禁止在 PR 中粘贴真实凭据或港口私有数据。
- 发布标签：必须通过 CI、依赖审查、SBOM、构建来源证明和视觉资产权利确认。

## 决策原则

实测、公开数据、授权数据、模型估算、压力扰动和合成场景必须分开标识。演示效果不能覆盖数据
来源和算法边界。无法由代码证明的港口授权、资产权利和生产安全审批由部署方负责并留档。

## 模型生命周期

新算法先作为候选基线，不自动替换已有策略。验证段决定默认候选，最终测试只用于一次独立报告；
生产部署还需在外部模型注册表完成审批、签名、灰度和回滚。仓库内置接口永不代表港口授权下发。
