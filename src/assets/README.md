# Source Assets

This folder stores TypeScript-facing asset manifests and visual layout constants.

Browser-served project-original vectors live in `public/assets` and use stable absolute URLs such as
`/assets/backgrounds/malacca-operations-grid.svg`.
